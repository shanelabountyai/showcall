# Next

**S-3 — role-filtered call sheets** (`docs/backlog.md`). Per-role projection
of the run sheet (`src/runsheet/cascade.ts` → `loadRunSheet`), versioned
issues recording the run-sheet agenda version, changed-since-last-issue diff,
no-over-disclosure asserted by sweep (hard rule 7), PDF. Needs a way to say
which roles a cue concerns — decide that first. Cue model: D-006.
