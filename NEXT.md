# Next

**S-2 — cue graph + cascade** (`docs/backlog.md`). Run-sheet cues per
room/day derived from the published agenda plus production items (doors,
walk-ins, strikes); each cue anchors to another with an offset so times
derive; a time change previews its cascade then commits atomically; detect
slack and impossible compression. TDD: cascade fixtures first. Record the
agenda version each run sheet is built from (D-003).
